# Local development only — DO NOT commit this file.
# For Streamlit Cloud: add these same keys in
#   App Dashboard → ⋮ → Settings → Secrets
# (paste the lines below as-is, without the [section] headers)

SMTP_HOST     = "smtp.gmail.com"
SMTP_PORT     = 587
SMTP_USER     = "your.email@gmail.com"
SMTP_PASSWORD = "your_gmail_app_password"
FROM_NAME     = "Office Project Manager"
